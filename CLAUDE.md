# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Warpine is an OS/2 compatibility layer for Linux that runs 32-bit OS/2 (LX format) and 16-bit OS/2 1.x (NE format) binaries natively using KVM for hardware-accelerated CPU emulation. Analogous to WINE but for OS/2 instead of Windows.

**License:** GPL-3.0-only

**Engineering policy — near-clean-room, blackbox implementation:** All API behaviour is derived from public documentation only (IBM *Control Program Programming Reference*, OS/2 Warp 4 Toolkit headers, IBM Developer Connection materials, osFree/ReactOS reference). No IBM-proprietary DLL binaries, no ROM dumps, and no disassembly of original OS/2 system libraries are used. Observable behaviour of Open Watcom-compiled OS/2 applications is the primary test oracle.

**Status:** Phases 1–7 (baseline) complete. Key milestones: NE (16-bit OS/2 1.x) execution complete — `ne_exec.rs` loads NE segments into GDT-tiled memory; SDL2 VGA text renderer (640×400, CP437, CGA palette, blinking cursor); MMPM/2 audio expanded — `MCI_SEEK`, `MCI_SET volume`, `MCI_NOTIFY` non-blocking play, `MCI_RECORD` stub; GPI drawing primitives complete — `GpiBox`, `GpiLine`, `GpiCharString`, `GpiQueryFontMetrics`, `GpiQueryTextBox`, mix modes, colour tracking (20+ ordinals); PM core window management complete — `WinCreateWindow` with built-in controls (WC_BUTTON/WC_STATIC/WC_SCROLLBAR/WC_ENTRYFIELD/WC_MLE/WC_LISTBOX), `WinSetWindowPos`, `WinEnableWindow`, `WinQueryWindowPos`, `WinMessageBox` real modal dialog via SDL2 `show_message_box`; DLL loader fully recursive with reference counting — `load_dll_impl` loads transitive dependencies, `DosFreeModule` frees at ref-count zero; Far16 thunk bypass handles LSS+JMP variant; pre-commit hook enforces `cargo test + clippy`. 222 API entry points covered (129-entry static registry + 93 sub-dispatcher ordinals). Unicode-internal architecture begun: `DosSetProcessCp` stores active codepage in `SharedState::active_codepage` (AtomicU32); `codepage.rs` provides `cp_decode`/`cp_encode` using embedded tables (CP437/850/852) and `encoding_rs` (Windows/DBCS codepages); `read_guest_string` decodes all guest strings through the active codepage. DLL INITTERM call injection — `_DLL_InitTerm(hmod, 0)` injected at load time via `ApiResult::CallGuest`; `_DLL_InitTerm(hmod, 1)` injected at unload time by `DosFreeModule`; `FrameKind::InitTerm`/`InitTermUnload` dispatch CALLBACK_RET_TRAP; `ERROR_INIT_ROUTINE_FAILED` on load failure; pages freed after unload call. `DosMapCase`/`NlsMapCase` full case mapping — SBCS: `cp_map_case_upper(byte, cp)` handles CP437/850/852 via upper-half tables, CP866 (IBM Cyrillic DOS) and CP1250–1258 via `encoding_rs`; multi-char results (ß→SS) unchanged. DBCS: `map_case_guest_buf()` scans lead+trail pairs using `is_dbcs_lead_byte`/`decode_dbcs`/`cp_to_encoding`, applies Unicode uppercase, re-encodes if the result is still a 2-byte pair; orphaned lead bytes and codepoints without a 1:1 uppercase are left unchanged. Path strings fully codepage-aware — input via `read_guest_string` → `cp_decode` (all 57 call sites); write-back via `cp_encode` in `DosQueryCurrentDir`, `dos_enum_attribute` (DENA1), `dos_query_path_info` (FEA2LIST), and `write_filefindbuf3_multi`. VIO output Unicode-internal — `VioManager::buffer: Vec<(char, u8)>` stores decoded Unicode codepoints; `decode_vio_byte(b, cp)` converts at write time; `get_glyph_for_char(ch)` binary-searches GNU Unifont 17 SBCS table (7282 half-width glyphs generated by `build.rs`); `VioReadCellStr` re-encodes back to active codepage bytes. PM strings codepage-aware — `WinQueryWindowText`/`WinQueryDlgItemText` encode output via `cp_encode`; `GpiCharString`/`GpiCharStringAt` decode input via `cp_decode` (was `from_utf8_lossy`). UCONV.DLL Unicode conversion — `uconv.rs` implements `UniCreateUconvObject`/`UniFreeUconvObject`/`UniUconvToUcs`/`UniUconvFromUcs`/`UniMapCpToUcsCp` (ordinals 1–4, 6); `UconvManager` (handle→codepage HashMap) in `SharedState::uconv_mgr`; `UCONV_BASE = 12288`, `STUB_AREA_SIZE` extended to 16384; UCONV added to `BUILTIN_MODULES` and `resolve_import`. DBCS cell annotation — `VioManager::raw_bytes: Vec<u8>` parallel to `buffer` (original guest byte per cell); `VgaTextBuffer` gains `raw_bytes` and `cell_kind: Vec<CellKind>` (`Sbcs`/`DbcsLead`/`DbcsTail`) populated by `annotate_dbcs()` at snapshot time; `annotate_dbcs(raw_bytes, codepage, cols)` left-to-right scan uses `dbcs_lead_ranges()`; SBCS fast-path. DBCS Unicode decode — `decode_dbcs(lead, trail, cp) -> char` in `codepage.rs` delegates to `cp_decode(&[lead, trail], cp)` (encoding_rs); `VgaTextBuffer::snapshot()` fixes up DbcsLead cells from U+FFFD to correct codepoints; CP936 (GBK) added to `cp_to_encoding()`. DBCS render path — `Sdl2TextRenderer::render_frame()` uses `while col` loop dispatching on `CellKind`; DbcsLead renders 16-wide `[u8;32]` glyph via `get_glyph_dbcs(ch)` (binary search over `$OUT_DIR/font_unifont_wide.bin`; 49,804 entries emitted by `build.rs`); DbcsTail skips; Sbcs unchanged; cursor overlay 16-wide for DbcsLead. DBCS Phase B complete — B6: `dos_query_dbcs_env()` shared implementation for `NlsGetDBCSEv` and `DosQueryDBCSEnv` (ord 373) returns real `(first,last)` lead-byte pairs + `(0,0)` terminator from `dbcs_lead_ranges()`; B7: `VioCheckCharType` (VIO ord 39) left-to-right row scan via `annotate_dbcs()`, returns 0/2/3, `ERROR_VIO_ROW`/`ERROR_VIO_COL` (426/427) for OOB; B8: SDL2 `TextInput` handler re-encodes IME-composed UTF-8 text via `cp_encode(active_codepage)` into `kbd_queue` as `KbdKeyInfo{scan=0}` entries, pure ASCII skipped. 264 API entry points covered (135-entry static registry + 129 sub-dispatcher ordinals). `DosLoadModule`/`DosQueryProcAddr` builtin module support — `dos_load_module` intercepts `BUILTINS` list (DOSCALLS/PMWIN/VIOCALLS/MDM/UCONV/etc.) before host path search, registers a synthetic `LoadedDll { is_builtin: true }` on first call (reuses handle on repeat calls); `dos_query_proc_addr` detects `is_builtin` and computes the thunk address via `resolve_import(mod_name, ordinal)` instead of looking up an empty export table; `LoadedDll::is_builtin: bool` field added to `managers.rs`; eliminates `DosLoadModule('UCONV') not found on host` runtime warning; 3 new unit tests. Structured Exception Handling (SEH) complete — `seh.rs` implements the full per-thread handler chain; `TIB+0x00` (tib_pexchain) initialised to `XCPT_CHAIN_END` (0xFFFFFFFF) in `setup_guest()`; `DosSetExceptionHandler`/`DosUnsetExceptionHandler` maintain the chain by updating TIB; hardware IDT exceptions (vectors 0–31) dispatch through the chain via `try_hw_exception_dispatch()` → `ApiResult::ExceptionDispatch` → `FrameKind::ExceptionHandler`; `XCPT_CONTINUE_EXECUTION` restores full CPU context from the CONTEXTRECORD; `XCPT_CONTINUE_SEARCH` walks to the next handler or crashes on chain exhaustion; `DosRaiseException` emits `ApiResult::ExceptionDispatch` from the API path; `DosUnwindException` truncates the TIB chain to the target record; `EXCEPTIONREPORTRECORD`/`CONTEXTRECORD` written to dynamically-allocated guest memory and freed after handling; `xcpt_code_for_vector()` maps x86 vectors to OS/2 XCPT_* codes; `setup_exception_dispatch()` (free function in vcpu.rs) is the single setup site used by both the IDT-exception and API-call paths. Builtin CMD.EXE shell — `cmd.rs` implements a native Rust OS/2 command shell; `DosExecPgm("CMD.EXE")`/`DosExecPgm("OS2SHELL.EXE")` intercepted in `dos_exec_pgm()` → `Loader::run_builtin_cmd()`; dual-mode keyboard input (SDL2 condvar + termios); line editor with backspace/ESC/Up/Down history (20 entries); built-ins: DIR, CD, SET, ECHO, CLS, VER, TYPE, MD, RD, DEL, HELP, EXIT, drive-letter switch; external program execution; .CMD script interpreter; /C and /K flags; I/O redirection (`>` truncate, `>>` append, `<` stdin) and pipeline (`|`) via `parse_command()`/`execute_pipeline()`/`execute_segment()` and `OutputSink`/`InputSource` `RefCell` fields; `spawn_external` method (sink-aware child relay); `samples/cmd_test/test.cmd` exercises all features; 35 new unit tests total. `lx_link` ELF-to-LX linker (`src/bin/lx_link.rs`) — standalone binary reads ELF `.o`/`.rlib`/`.a` files, merges code/data sections, resolves OS/2 API imports via `targets/os2api.def`, emits valid MZ+LX executables; modules: `args` (rustc flag parsing), `def_parser`, `elf_reader`, `linker_state`, `lx_writer`; 11 unit tests; `object = "0.36"` dependency. 516 unit tests (486 warpine + 18 gen_api + 11 lx_link + 1 submenu) + 10 integration tests (including `test_rust_hello`). RT_* resource type constants corrected to OS/2 numbering: `RT_MENU=3`, `RT_DIALOG=4`, `RT_STRING=5`, `RT_ACCELTABLE=8` (previously wrong Windows numbers). MIS_* constants corrected to OS/2 `<pmwin.h>` values: `MIS_SEPARATOR=0x0004`, `MIS_OWNERDRAW=0x0008`, `MIS_MULTMENU=0x0020`, `MIS_SYSCOMMAND=0x0040`, `MIS_HELP=0x0080`, etc. PM Menu System complete — `parse_menu_template()` parses binary MENUTEMPLATE (RT_MENU=3) resources; Open Watcom wrc uses a **count-based** 10-byte header (`u32 total_size + u16 codepage + u16 reserved + u16 item_count`) — items start at offset 10 (not the old 8+cbInfo); each SUBMENU item is followed by an identical 10-byte child-block header with `child_count`; MIS_END (0x8000) still supported as fallback for count=0 (hand-built templates); `parse_menu_items` is count-based with sub-header skip for SUBMENU children; `WinLoadMenu` (778) parses RT_MENU resource and registers a menu pseudo-window with stored `MenuItem` tree; `WinSetMenu` (857) associates a menu hwnd with a frame window; `WinCreateMenu` (907) parses an inline template pointer. PM Menu Interaction complete — `WinDefWindowProc` (911) intercepts `WM_BUTTON1DOWN` on frame windows with a menu: forwards clicks in the menu bar or in an open dropdown to `dispatch_builtin_control(menu_hwnd, WM_BUTTON1DOWN, mp1, 0)`; `#Menu` `WM_BUTTON1DOWN` handler hit-tests top-level items (by accumulated label width) to toggle dropdown state stored in `OS2Window::window_ulong[-1]` (0=closed, 1-based item index when open); `#Menu` `WM_MENUEND` closes the dropdown; `WM_PAINT` for `#Menu` highlights the open item in navy and draws a 120-px-wide dropdown box with child items (16 px/item, MIS_SEPARATOR renders as a horizontal rule); clicking a non-separator child posts `WM_COMMAND` with `CMDSRC_MENU=2` in the high word and the item id in the low word to the client window's message queue, then closes the dropdown. PM Dialog System complete — `parse_dlg_template()` parses binary DLGTEMPLATE (RT_DIALOG=4) resources; `create_dialog_from_template()` creates a frame window + child controls matching each `DlgItemInfo`; `WinDlgBox` (923) loads RT_DIALOG, creates dialog, posts `WM_INITDLG`, returns `ApiResult::DlgRunLoop`; `WinLoadDlg` (924) creates modeless dialog returning hwnd; `WinProcessDlg` (796) runs modal loop via `ApiResult::DlgRunLoop`; `WinDismissDlg` (729) sets `OS2Window::dialog_dismissed`/`dialog_result` and notifies condvar; `WinDefDlgProc` (910) handles WM_INITDLG/WM_COMMAND(DID_OK/DID_CANCEL)/WM_CLOSE; `WinSendDlgItemMsg` (903) routes to child by id. DlgRunLoop modal architecture — `ApiResult::DlgRunLoop { dlg_proc, hwnd_dlg, hmq }` + `FrameKind::DlgRunLoop` pair; `setup_dlg_dispatch()` free function (mirrors `setup_exception_dispatch()`): blocks on MQ condvar, dispatches next message to dlg_proc as `FrameKind::DlgRunLoop` callback; CALLBACK_RET_TRAP handler checks `dialog_dismissed` flag — if dismissed restores caller rip/rsp/rax, else re-dispatches. New constants: `WM_INITDLG=0x0021`, `WM_INITMENU=0x00E5`, `WM_MENUSELECT=0x0078`, `WM_MENUEND=0x0079`, `DID_OK=1`, `DID_CANCEL=2`, `DID_ERROR=0xFFFF`, `MIS_TEXT=0x0001/MIS_BITMAP=0x0002/MIS_SEPARATOR=0x0004/MIS_OWNERDRAW=0x0008/MIS_SUBMENU=0x0010/MIS_MULTMENU=0x0020/MIS_SYSCOMMAND=0x0040/MIS_HELP=0x0080/MIS_STATIC=0x0100/MIS_END=0x8000`, `MIA_NODISMISS/FRAMED/CHECKED/DISABLED/HILITED`. New `OS2Window` fields: `menu_items: Vec<MenuItem>`, `menu_hwnd: u32`, `dialog_dismissed: bool`, `dialog_result: u32`. New sample: `samples/pm_menu_dlg_test/` (MENU + DLGTEMPLATE resource, Open Watcom build). Ordinal Table Canonical Build Tool — `gen_api` (`src/bin/gen_api.rs`) uses `targets/os2api.def` as the single authoritative source for all OS/2 API ordinals; five subcommands: `show`, `check` (drift-detects against `api_trace.rs`), `gen-trace` (emits `ordinal_to_name()` + `module_for_ordinal()` Rust source), `gen-def`, `validate-doc` (cross-checks def against `doc/os2_ordinals.md` OW snapshot — exact-matches 32-bit names, auto-detects 16-bit aliases, reports mismatches); 18 unit tests; `check` now reports clean (`✓ os2api.def is consistent — 162 entries`); MDM ordinal bug fixed in def file (MDM.3 was mciSendString; corrected to MDM.2); MSG.3/MSG.6/MDM.3/MDM.4 added to def; `api_trace.rs` `ordinal_to_name()` now covers KBDCALLS/VIOCALLS + 6 previously unnamed DOSCALLS + NLS.9; marked auto-generated from `os2api.def`. Rust Guest Toolchain complete — `crates/warpine-os2-sys`: raw `extern "C"` DOSCALLS bindings (DosExit/Write/Read/Open/Close/AllocMem/FreeMem/SetMem/CreateThread/WaitThread/KillThread/GetInfoBlocks/Sleep/GetDateTime/QuerySysInfo + event/mutex semaphore APIs) + `extern "stdcall"` VIOCALLS/KBDCALLS bindings with reversed arg order for Pascal stack layout (VioWrtTTY/VioGetCurPos/VioSetCurPos/KbdCharIn/KbdGetStatus) + types `USHORT`/`HVIO`/`HKBD`/`PFNTHREAD`/`DATETIME`/`KBDKEYINFO`/`KBDINFO`; `crates/warpine-os2-rt`: `_start` + panic handler + global allocator; `crates/warpine-os2`: safe wrappers `mod file` (write_stdout/write_stderr), `mod memory` (alloc/free/set_mem), `mod process` (exit), `mod thread` (sleep/create/wait/kill), `mod vio` (write_tty/get_cur_pos/set_cur_pos); `samples/rust_hello` end-to-end Rust→LX hello world. See `doc/TODOs.md` for the full roadmap.

## Build & Run

```bash
cargo build                              # Debug build
cargo run -- <path_to_os2_executable>    # Run an OS/2 binary
cargo run -- samples/hello/hello.exe     # Example: run hello world
cargo test                               # Unit tests (486 warpine + 18 gen_api + 11 lx_link = 515 unit tests: LX/NE parsers, VFS, managers, MMPM, API registry, HeadlessRenderer, scan codes, text renderer, DLL loader, crash dump, API ring buffer, GPI, PM controls, codepage, INITTERM, DosMapCase SBCS+CP866+DBCS, DBCS B1–B8, builtin DLL loading, SEH chain+dispatch, CMD.EXE shell, I/O redirection+pipeline, lx-link ELF-to-LX linker, gen_api ordinal table + validate-doc tool, PM menu/dialog system + MENUTEMPLATE submenu parsing + menu bar click/dropdown) + 10 integration tests
cargo clippy -- -D warnings             # Lint — must pass with zero warnings before every commit/release
```

**Prerequisites:** Linux with KVM enabled (`/dev/kvm`), x86_64 CPU with VT-x/AMD-V, Rust 2024 edition, `libsdl2-dev` (for PM/GUI window support).

**Rust Guest Toolchain** (building OS/2 guest binaries in Rust):
```bash
# Nightly + rust-src required; install once:
rustup toolchain install nightly --component rust-src

# Build and install the ELF-to-LX linker:
cargo build --bin lx_link && cp target/debug/lx_link ~/.cargo/bin/lx-link

# Build a guest crate (example: samples/rust_hello):
cd samples/rust_hello
cargo +nightly build \
  -Z build-std=core,alloc \
  -Z build-std-features=compiler-builtins-mem \
  -Z json-target-spec \
  --target ../../targets/i686-warpine-os2.json

# Run the resulting LX binary:
cargo run --manifest-path ../../Cargo.toml -- \
  samples/rust_hello/target/i686-warpine-os2/debug/rust_hello.exe
```
`targets/i686-warpine-os2.json` — custom target spec (`i686-unknown-none`, `panic=abort`, linker=`lx-link`, only `R_386_32` relocations emitted).
`targets/os2api.def` — OS/2 API ordinal map consumed by `lx-link` to resolve imports (DOSCALLS/PMWIN/VIOCALLS/KBDCALLS/NLS/UCONV/MDM etc.).

**Phase C — Rust Guest Crates** (standalone; NOT part of the host workspace):
- `crates/warpine-os2-sys/` — `#![no_std]` raw OS/2 API bindings (`extern "C"` declarations for DosExit/DosWrite/DosRead/DosOpen/DosClose/DosAllocMem/DosFreeMem/DosSetMem) with OS/2 type aliases (APIRET/ULONG/HFILE/PVOID/PCSZ).
- `crates/warpine-os2-rt/` — `#![no_std]` runtime: `_start` entry point (calls `os2_main() -> u32` then `DosExit`), panic handler, and global allocator backed by `DosAllocMem`/`DosFreeMem`.
- `crates/warpine-os2/` — `#![no_std]` ergonomic wrappers: `mod file` (`write_stdout`/`write_stderr`), `mod memory` (`alloc`/`free`/`set_mem`), `mod process` (`exit`), `mod thread` (`sleep`/`create`/`wait`/`kill`), `mod vio` (`write_tty`/`get_cur_pos`/`set_cur_pos`).
- `samples/rust_hello/` — "Hello from Rust on Warpine!" demonstration using the above crates; `extern crate warpine_os2_rt` forces the runtime to link in.

**Sample OS/2 apps** are in `samples/` (hello, alloc_test, file_test, pipe_test, rust_hello, 4os2, etc.). Build Watcom samples with Open Watcom: `./vendor/setup_watcom.sh` then `make -C samples/<name>`. For 4OS2: `cd samples/4os2 && ./fetch_source.sh && make`. Build Rust samples as shown above.

## Architecture

### Execution flow

1. **Parse** — `main.rs` calls `LxFile::open()` to parse the MZ+LX executable (headers, object table, page map, fixup records, imports)
2. **Load** — `Loader::load()` maps executable pages into KVM guest memory (128MB) and applies relocations via `apply_fixups()`
3. **Execute** — `Loader::run()` sets up TIB/PIB, creates a vCPU, and enters the VMEXIT loop (`run_vcpu()`)
4. **API thunking** — Guest API calls hit INT 3 breakpoints at magic addresses (MAGIC_API_BASE = 0x01000000), causing VMEXIT_DEBUG → `handle_api_call()` dispatches to host-side Rust implementations by ordinal number

### Key modules

- **`src/lx/`** — LX executable format parser (`header.rs` for binary structures, `lx.rs` for orchestration). Unit tests live here.
- **`src/ne/`** — NE (New Executable) format parser for OS/2 1.x 16-bit apps (`header.rs` for structures, `ne.rs` for orchestration). 16 unit tests.
- **`src/loader/`** — The core: KVM VMM, memory manager, handle manager, semaphore manager, queue manager, VMEXIT loop, and OS/2 API handler functions. Split into `mod.rs` (loader core + `SharedState` with `DllManager`; `ApiResult` enum: `Normal`/`Callback`/`CallGuest{addr,hmod,phmod,object_bases}`/`ExceptionDispatch{handler_addr,exc_report,reg_rec,ctx_record,saved,next_handler,guest_allocs}`; `FrameKind`: `PmCallback`/`InitTerm{hmod,phmod}`/`InitTermUnload{hmod,object_bases}`/`ExceptionHandler{saved,next_handler,exc_report,ctx_record,guest_allocs}`; `FaultContext`; `CallbackFrame`), `vcpu.rs` (vCPU thread, VMEXIT loop, GDB integration), `vm_backend.rs` (`VmBackend`/`VcpuBackend` traits), `kvm_backend.rs` (KVM implementation), `guest_mem.rs` (safe typed guest memory access), `descriptors.rs` (GDT/IDT setup, `resolve_import()`), `api_registry.rs` (static sorted API thunk table, 135 entries), `api_dispatch.rs` (registry lookup + sub-dispatcher routing), `api_trace.rs` (ordinal-to-name/module/arg-names helpers + `format_call()` strace formatter), `api_ring.rs` (256-entry API call ring buffer for crash dumps), `crash_dump.rs` (structured crash reports on fatal VMEXITs), `doscalls.rs`, `viocalls.rs`, `kbdcalls.rs`, `console.rs`, `pm_win.rs`, `pm_gpi.rs`, `pm_types.rs` (PM data types and `WindowManager`), `stubs.rs` (includes `DosLoadModule`/`DosQueryProcAddr`; `dos_load_module` intercepts `BUILTINS` list and registers synthetic `LoadedDll{is_builtin:true}` entries; returns `ApiResult::CallGuest` when INITTERM present), `process.rs`, `managers.rs` (`DllManager`/`LoadedDll` with `initterm_addr: Option<u32>` and `is_builtin: bool` — distinguishes host-loaded DLLs from emulated builtins for `DosQueryProcAddr` thunk resolution), `constants.rs`, `mutex_ext.rs` (poison-recovering lock), `mmpm.rs` (MMPM/2 audio: MmpmManager, beep_tone, mciSendCommand/String), `lx_loader.rs` (`load_dll()`, `find_dll_path()`), `ne_exec.rs` (NE loader: `load_ne()`, `setup_guest_ne()`, `handle_ne_api_call()`, `ne_api_arg_bytes()`), `ipc.rs` (semaphores, pipes, queues), `locale.rs` (`Os2Locale`: country/codepage), `codepage.rs` (`cp_decode`/`cp_encode`/`cp_to_encoding`/`decode_dbcs`: guest byte↔UTF-8 conversion for all supported OS/2 codepages via embedded tables + `encoding_rs`; `decode_dbcs(lead, trail, cp)` decodes DBCS pairs for CP932/936/949/950), `uconv.rs` (`UconvManager`, `UniCreateUconvObject`/`UniFreeUconvObject`/`UniUconvToUcs`/`UniUconvFromUcs`/`UniMapCpToUcsCp` — UCONV.DLL emulation, ordinals at UCONV_BASE+1..+6), `seh.rs` (`xcpt_code_for_vector`, `write_exception_report`, `write_context_record`, `read_context_record`, `alloc_exception_records`, `free_exception_records`, `try_hw_exception_dispatch`, `dos_raise_exception`, `dos_unwind_exception` — OS/2 SEH chain), `cmd.rs` (`CmdShell`: interactive line editor + built-ins DIR/CD/SET/ECHO/CLS/VER/TYPE/MD/RD/DEL/HELP/EXIT; `.CMD` script interpreter; I/O redirection (`>`, `>>`, `<`) and pipelines (`|`) via `OutputSink`/`InputSource` `RefCell` fields + `parse_command()`/`execute_pipeline()`/`execute_segment()`; `spawn_external` method replaces `spawn_os2_program` free function; `Loader::run_builtin_cmd()` entry point — intercepted from `dos_exec_pgm` for CMD.EXE/OS2SHELL.EXE), `gdb_stub.rs` (`GdbState`, `WarpineTarget`, `GdbBlockingEventLoop`, `launch_gdb_stub` — GDB RSP over TCP), `vfs.rs` (VfsBackend trait, DriveManager), `vfs_hostdir.rs` (HostDirBackend) — see developer guide for VFS architecture.
- **`src/bin/lx_link.rs`** — ELF-to-LX linker: reads ELF `.o`/`.rlib`/`.a` files produced for `i686-warpine-os2`, merges code/data sections, resolves imports via `targets/os2api.def` (embedded via `include_str!` as fallback), emits a valid LX executable (MZ stub + LX header + object table + page map + fixup records + data pages). Modules: `args` (CLI flag parsing, silently ignores rustc's extra flags), `def_parser` (MODULE.ORDINAL symbol map), `elf_reader` (ELF sections/symbols/relocations via `object` crate), `linker_state` (section merging, VA assignment, symbol table), `lx_writer` (LX binary serialisation, relocation patching, fixup record emission). 11 unit tests. Dependency: `object = "0.36"`.
- **`src/bin/gen_api.rs`** — Ordinal Table Canonical Build Tool: `targets/os2api.def` is the single source of truth; subcommands `show` / `check` / `gen-trace` / `gen-def`; `check` text-scans `api_trace.rs` match arms to detect drift between def and trace; `gen-trace` emits `ordinal_to_name()` + `module_for_ordinal()` Rust source. 15 unit tests.
- **`src/api.rs`** — Small module with `DosWrite`/`DosExit` implementations and FFI bridge stubs.
- **`src/gui/`** — GUI and text-mode backends. `message.rs`: `GUIMessage` channel. `renderer.rs`: `PmRenderer` trait + `run_pm_loop()`. `render_utils.rs`: geometry helpers. `headless.rs`: `HeadlessRenderer`. `sdl2_renderer.rs`: `Sdl2Renderer` (PM SDL2 backend), `push_msg`, scancode/VK tables. `text_renderer.rs`: `TextModeRenderer` trait, `Sdl2TextRenderer` (640×400 VGA text window), `HeadlessTextRenderer`, `run_text_loop()`, `get_glyph_for_char()` (SBCS binary search), `get_glyph_dbcs()` (DBCS 16×16 binary search over `font_unifont_wide.bin`), `CGA_PALETTE`, `CellKind`, `annotate_dbcs()`.

### Concurrency model

Each OS/2 thread maps to a native Rust thread with its own KVM vCPU. `SharedState` wraps all managers in `Arc<Mutex<...>>` for cross-thread access. Semaphores use `Arc<(Mutex<State>, Condvar)>`.

### Important constants (in constants.rs)

- `MAGIC_API_BASE` (0x01000000) — API thunk stub area
- `EXIT_TRAP_ADDR` (0x010003FF) — Special exit breakpoint
- `DYNAMIC_ALLOC_BASE` (0x02000000) — Guest memory allocation pool
- `TIB_BASE` (0x00090000), `PIB_BASE` (0x00091000) — Thread/Process info blocks (must stay below 0x100000 for 16-bit segment arithmetic)
- `KBDCALLS_BASE` (4096), `VIOCALLS_BASE` (5120), `SESMGR_BASE` (6144), `NLS_BASE` (7168), `MSG_BASE` (8192), `MDM_BASE` (10240), `UCONV_BASE` (12288) — Ordinal offset bases for subsystem dispatch; `STUB_AREA_SIZE` (16384)

### OS/2 PIB layout (key offsets)

- `+0x00` pib_ulpid, `+0x04` pib_ulppid, `+0x08` pib_hmte, `+0x0C` pib_pchcmd, `+0x10` pib_pchenv

## Conventions

- **Modular separation:** Keep API emulations, loader logic, and format parsing in their respective modules.
- **Every new feature or bug fix must include tests.** High coverage required for the LX parser and API thunks.
- **Unsafe code** is expected for KVM/guest memory operations but must have clear safety justification and enforce guest memory isolation. All code involving pointer arithmetic or guest-to-host transitions must be reviewed for buffer overflows.
- **Use named constants** — no magic numbers for GDT entries, segment selectors, or API ordinals.
- **Stubbing pattern** — unimplemented APIs should use clear stubs to allow incremental progress.
- OS/2 paths (`C:\path`) are translated to Unix paths by replacing backslashes. OS/2 error codes (u32) are returned in RAX.
- Ensure this document is updated to reflect any changes in the workflow and maintain consistency.

### While Planning, Refactoring & Doing Code Review

- When a feature requirement is unclear or ambiguous, seek clarification on definition and scope rather than guessing.

### While Coding

- **Always** try to write test sample OS/2 applications (in `samples/`) to verify assumptions, evaluations.

### After Every Change

1. **Always** update all relevant documents (`README.md`, `doc/*.md` and this file `CLAUDE.md`...)
2. **Always** add essential but missing tests to improve test coverage and ensure code quality
3. **Always** check if there is any missing or incomplete test
4. **Always** run `cargo clippy -- -D warnings` and fix all warnings before committing
4. Remove the finishied tasks from TODOs
5. When a bug is discovered, **always** check for similar issues across the project after applying the fix

### Release Engineering

When creating a new release:

1. Update `CHANGELOG.md` with the new version entry (follow [Keep a Changelog](https://keepachangelog.com/) format)
2. Update `version` in `Cargo.toml` to match the new tag version
3. Commit, push, and create the git tag (e.g. `v1.1.21`)
4. Push the tag (`git push --tags`)
5. Create the GitHub release via `gh release create`

### Code Organization

- Commit by topic — group related files per commit

### Security Rules

- **Always** take care about hypervisor escape prevention
- Memory safety is top priority
