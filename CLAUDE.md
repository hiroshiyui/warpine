# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Warpine is an OS/2 compatibility layer for Linux that runs 32-bit OS/2 (LX format) and 16-bit OS/2 1.x (NE format) binaries natively using KVM for hardware-accelerated CPU emulation. Analogous to WINE but for OS/2 instead of Windows.

**License:** GPL-3.0-only

**Engineering policy — near-clean-room, blackbox implementation:** All API behaviour is derived from public documentation only (IBM *Control Program Programming Reference*, OS/2 Warp 4 Toolkit headers, IBM Developer Connection materials, osFree/ReactOS reference). No IBM-proprietary DLL binaries, no ROM dumps, and no disassembly of original OS/2 system libraries are used. Observable behaviour of Open Watcom-compiled OS/2 applications is the primary test oracle.

**Status:** Phases 1–7 (baseline) complete. Key milestones: NE (16-bit OS/2 1.x) execution complete — `ne_exec.rs` loads NE segments into GDT-tiled memory; SDL2 VGA text renderer (640×400, CP437, CGA palette, blinking cursor); MMPM/2 audio expanded — `MCI_SEEK`, `MCI_SET volume`, `MCI_NOTIFY` non-blocking play, `MCI_RECORD` stub; GPI drawing primitives complete — `GpiBox`, `GpiLine`, `GpiCharString`, `GpiQueryFontMetrics`, `GpiQueryTextBox`, mix modes, colour tracking (20+ ordinals); PM core window management complete — `WinCreateWindow` with built-in controls (WC_BUTTON/WC_STATIC/WC_SCROLLBAR/WC_ENTRYFIELD/WC_MLE/WC_LISTBOX), `WinSetWindowPos`, `WinEnableWindow`, `WinQueryWindowPos`, `WinMessageBox` real modal dialog via SDL2 `show_message_box`; DLL loader fully recursive with reference counting — `load_dll_impl` loads transitive dependencies, `DosFreeModule` frees at ref-count zero; Far16 thunk bypass handles LSS+JMP variant; pre-commit hook enforces `cargo test + clippy`. 222 API entry points covered (129-entry static registry + 93 sub-dispatcher ordinals). Unicode-internal architecture begun: `DosSetProcessCp` stores active codepage in `SharedState::active_codepage` (AtomicU32); `codepage.rs` provides `cp_decode`/`cp_encode` using embedded tables (CP437/850/852) and `encoding_rs` (Windows/DBCS codepages); `read_guest_string` decodes all guest strings through the active codepage. DLL INITTERM call injection — `_DLL_InitTerm(hmod, 0)` injected at load time via `ApiResult::CallGuest`; `_DLL_InitTerm(hmod, 1)` injected at unload time by `DosFreeModule`; `FrameKind::InitTerm`/`InitTermUnload` dispatch CALLBACK_RET_TRAP; `ERROR_INIT_ROUTINE_FAILED` on load failure; pages freed after unload call. `DosMapCase`/`NlsMapCase` SBCS case mapping — `cp_map_case_upper(byte, cp)` in `codepage.rs` handles CP437/850/852 via upper-half tables and CP1250–1258 via `encoding_rs`; multi-char results (ß→SS) unchanged. Path strings fully codepage-aware — input via `read_guest_string` → `cp_decode` (all 57 call sites); write-back via `cp_encode` in `DosQueryCurrentDir`, `dos_enum_attribute` (DENA1), `dos_query_path_info` (FEA2LIST), and `write_filefindbuf3_multi`. VIO output Unicode-internal — `VioManager::buffer: Vec<(char, u8)>` stores decoded Unicode codepoints; `decode_vio_byte(b, cp)` converts at write time; `get_glyph_for_char(ch)` binary-searches GNU Unifont 17 SBCS table (7282 half-width glyphs generated by `build.rs`); `VioReadCellStr` re-encodes back to active codepage bytes. PM strings codepage-aware — `WinQueryWindowText`/`WinQueryDlgItemText` encode output via `cp_encode`; `GpiCharString`/`GpiCharStringAt` decode input via `cp_decode` (was `from_utf8_lossy`). UCONV.DLL Unicode conversion — `uconv.rs` implements `UniCreateUconvObject`/`UniFreeUconvObject`/`UniUconvToUcs`/`UniUconvFromUcs`/`UniMapCpToUcsCp` (ordinals 1–4, 6); `UconvManager` (handle→codepage HashMap) in `SharedState::uconv_mgr`; `UCONV_BASE = 12288`, `STUB_AREA_SIZE` extended to 16384; UCONV added to `BUILTIN_MODULES` and `resolve_import`. DBCS cell annotation — `VioManager::raw_bytes: Vec<u8>` parallel to `buffer` (original guest byte per cell); `VgaTextBuffer` gains `raw_bytes` and `cell_kind: Vec<CellKind>` (`Sbcs`/`DbcsLead`/`DbcsTail`) populated by `annotate_dbcs()` at snapshot time; `annotate_dbcs(raw_bytes, codepage, cols)` left-to-right scan uses `dbcs_lead_ranges()`; SBCS fast-path. DBCS Unicode decode — `decode_dbcs(lead, trail, cp) -> char` in `codepage.rs` delegates to `cp_decode(&[lead, trail], cp)` (encoding_rs); `VgaTextBuffer::snapshot()` fixes up DbcsLead cells from U+FFFD to correct codepoints; CP936 (GBK) added to `cp_to_encoding()`. DBCS render path — `Sdl2TextRenderer::render_frame()` uses `while col` loop dispatching on `CellKind`; DbcsLead renders 16-wide `[u8;32]` glyph via `get_glyph_dbcs(ch)` (binary search over `$OUT_DIR/font_unifont_wide.bin`; 49,804 entries emitted by `build.rs`); DbcsTail skips; Sbcs unchanged; cursor overlay 16-wide for DbcsLead. DBCS Phase B complete — B6: `dos_query_dbcs_env()` shared implementation for `NlsGetDBCSEv` and `DosQueryDBCSEnv` (ord 373) returns real `(first,last)` lead-byte pairs + `(0,0)` terminator from `dbcs_lead_ranges()`; B7: `VioCheckCharType` (VIO ord 39) left-to-right row scan via `annotate_dbcs()`, returns 0/2/3, `ERROR_VIO_ROW`/`ERROR_VIO_COL` (426/427) for OOB; B8: SDL2 `TextInput` handler re-encodes IME-composed UTF-8 text via `cp_encode(active_codepage)` into `kbd_queue` as `KbdKeyInfo{scan=0}` entries, pure ASCII skipped. 262 API entry points covered (136-entry static registry + 126 sub-dispatcher ordinals). `DosLoadModule`/`DosQueryProcAddr` builtin module support — `dos_load_module` intercepts `BUILTINS` list (DOSCALLS/PMWIN/VIOCALLS/MDM/UCONV/etc.) before host path search, registers a synthetic `LoadedDll { is_builtin: true }` on first call (reuses handle on repeat calls); `dos_query_proc_addr` detects `is_builtin` and computes the thunk address via `resolve_import(mod_name, ordinal)` instead of looking up an empty export table; `LoadedDll::is_builtin: bool` field added to `managers.rs`; eliminates `DosLoadModule('UCONV') not found on host` runtime warning; 3 new unit tests. 416 unit tests + 9 integration tests. See `doc/TODOs.md` for the full roadmap.

## Build & Run

```bash
cargo build                              # Debug build
cargo run -- <path_to_os2_executable>    # Run an OS/2 binary
cargo run -- samples/hello/hello.exe     # Example: run hello world
cargo test                               # Unit tests (416 tests: LX/NE parsers, VFS, managers, MMPM, API registry, HeadlessRenderer, scan codes, text renderer, DLL loader, crash dump, API ring buffer, GPI, PM controls, codepage, INITTERM, DosMapCase, DBCS B1–B8, builtin DLL loading) + 9 integration tests
cargo clippy -- -D warnings             # Lint — must pass with zero warnings before every commit/release
```

**Prerequisites:** Linux with KVM enabled (`/dev/kvm`), x86_64 CPU with VT-x/AMD-V, Rust 2024 edition, `libsdl2-dev` (for PM/GUI window support).

**Sample OS/2 apps** are in `samples/` (hello, alloc_test, file_test, pipe_test, 4os2, etc.). Build them with Open Watcom: `./vendor/setup_watcom.sh` then `make -C samples/<name>`. For 4OS2: `cd samples/4os2 && ./fetch_source.sh && make`.

## Architecture

### Execution flow

1. **Parse** — `main.rs` calls `LxFile::open()` to parse the MZ+LX executable (headers, object table, page map, fixup records, imports)
2. **Load** — `Loader::load()` maps executable pages into KVM guest memory (128MB) and applies relocations via `apply_fixups()`
3. **Execute** — `Loader::run()` sets up TIB/PIB, creates a vCPU, and enters the VMEXIT loop (`run_vcpu()`)
4. **API thunking** — Guest API calls hit INT 3 breakpoints at magic addresses (MAGIC_API_BASE = 0x01000000), causing VMEXIT_DEBUG → `handle_api_call()` dispatches to host-side Rust implementations by ordinal number

### Key modules

- **`src/lx/`** — LX executable format parser (`header.rs` for binary structures, `lx.rs` for orchestration). Unit tests live here.
- **`src/ne/`** — NE (New Executable) format parser for OS/2 1.x 16-bit apps (`header.rs` for structures, `ne.rs` for orchestration). 16 unit tests.
- **`src/loader/`** — The core: KVM VMM, memory manager, handle manager, semaphore manager, queue manager, VMEXIT loop, and OS/2 API handler functions. Split into `mod.rs` (loader core + `SharedState` with `DllManager`; `ApiResult` enum: `Normal`/`Callback`/`CallGuest{addr,hmod,phmod,object_bases}`; `FrameKind`: `PmCallback`/`InitTerm{hmod,phmod}`/`InitTermUnload{hmod,object_bases}`; `CallbackFrame`), `vcpu.rs` (vCPU thread, VMEXIT loop, GDB integration), `vm_backend.rs` (`VmBackend`/`VcpuBackend` traits), `kvm_backend.rs` (KVM implementation), `guest_mem.rs` (safe typed guest memory access), `descriptors.rs` (GDT/IDT setup, `resolve_import()`), `api_registry.rs` (static sorted API thunk table, 129 entries), `api_dispatch.rs` (registry lookup + sub-dispatcher routing), `api_trace.rs` (ordinal-to-name/module/arg-names helpers + `format_call()` strace formatter), `api_ring.rs` (256-entry API call ring buffer for crash dumps), `crash_dump.rs` (structured crash reports on fatal VMEXITs), `doscalls.rs`, `viocalls.rs`, `kbdcalls.rs`, `console.rs`, `pm_win.rs`, `pm_gpi.rs`, `pm_types.rs` (PM data types and `WindowManager`), `stubs.rs` (includes `DosLoadModule`/`DosQueryProcAddr`; `dos_load_module` intercepts `BUILTINS` list and registers synthetic `LoadedDll{is_builtin:true}` entries; returns `ApiResult::CallGuest` when INITTERM present), `process.rs`, `managers.rs` (`DllManager`/`LoadedDll` with `initterm_addr: Option<u32>` and `is_builtin: bool` — distinguishes host-loaded DLLs from emulated builtins for `DosQueryProcAddr` thunk resolution), `constants.rs`, `mutex_ext.rs` (poison-recovering lock), `mmpm.rs` (MMPM/2 audio: MmpmManager, beep_tone, mciSendCommand/String), `lx_loader.rs` (`load_dll()`, `find_dll_path()`), `ne_exec.rs` (NE loader: `load_ne()`, `setup_guest_ne()`, `handle_ne_api_call()`, `ne_api_arg_bytes()`), `ipc.rs` (semaphores, pipes, queues), `locale.rs` (`Os2Locale`: country/codepage), `codepage.rs` (`cp_decode`/`cp_encode`/`cp_to_encoding`/`decode_dbcs`: guest byte↔UTF-8 conversion for all supported OS/2 codepages via embedded tables + `encoding_rs`; `decode_dbcs(lead, trail, cp)` decodes DBCS pairs for CP932/936/949/950), `uconv.rs` (`UconvManager`, `UniCreateUconvObject`/`UniFreeUconvObject`/`UniUconvToUcs`/`UniUconvFromUcs`/`UniMapCpToUcsCp` — UCONV.DLL emulation, ordinals at UCONV_BASE+1..+6), `gdb_stub.rs` (`GdbState`, `WarpineTarget`, `GdbBlockingEventLoop`, `launch_gdb_stub` — GDB RSP over TCP), `vfs.rs` (VfsBackend trait, DriveManager), `vfs_hostdir.rs` (HostDirBackend) — see developer guide for VFS architecture.
- **`src/api.rs`** — Small module with `DosWrite`/`DosExit` implementations and FFI bridge stubs.
- **`src/gui/`** — GUI and text-mode backends. `message.rs`: `GUIMessage` channel. `renderer.rs`: `PmRenderer` trait + `run_pm_loop()`. `render_utils.rs`: geometry helpers. `headless.rs`: `HeadlessRenderer`. `sdl2_renderer.rs`: `Sdl2Renderer` (PM SDL2 backend), `push_msg`, scancode/VK tables. `text_renderer.rs`: `TextModeRenderer` trait, `Sdl2TextRenderer` (640×400 VGA text window), `HeadlessTextRenderer`, `run_text_loop()`, `get_glyph_for_char()` (SBCS binary search), `get_glyph_dbcs()` (DBCS 16×16 binary search over `font_unifont_wide.bin`), `CGA_PALETTE`, `CellKind`, `annotate_dbcs()`.

### Concurrency model

Each OS/2 thread maps to a native Rust thread with its own KVM vCPU. `SharedState` wraps all managers in `Arc<Mutex<...>>` for cross-thread access. Semaphores use `Arc<(Mutex<State>, Condvar)>`.

### Important constants (in constants.rs)

- `MAGIC_API_BASE` (0x01000000) — API thunk stub area
- `EXIT_TRAP_ADDR` (0x010003FF) — Special exit breakpoint
- `DYNAMIC_ALLOC_BASE` (0x02000000) — Guest memory allocation pool
- `TIB_BASE` (0x00090000), `PIB_BASE` (0x00091000) — Thread/Process info blocks (must stay below 0x100000 for 16-bit segment arithmetic)
- `KBDCALLS_BASE` (4096), `VIOCALLS_BASE` (5120), `SESMGR_BASE` (6144), `NLS_BASE` (7168), `MSG_BASE` (8192), `MDM_BASE` (10240), `UCONV_BASE` (12288) — Ordinal offset bases for subsystem dispatch; `STUB_AREA_SIZE` (16384)

### OS/2 PIB layout (key offsets)

- `+0x00` pib_ulpid, `+0x04` pib_ulppid, `+0x08` pib_hmte, `+0x0C` pib_pchcmd, `+0x10` pib_pchenv

## Conventions

- **Modular separation:** Keep API emulations, loader logic, and format parsing in their respective modules.
- **Every new feature or bug fix must include tests.** High coverage required for the LX parser and API thunks.
- **Unsafe code** is expected for KVM/guest memory operations but must have clear safety justification and enforce guest memory isolation. All code involving pointer arithmetic or guest-to-host transitions must be reviewed for buffer overflows.
- **Use named constants** — no magic numbers for GDT entries, segment selectors, or API ordinals.
- **Stubbing pattern** — unimplemented APIs should use clear stubs to allow incremental progress.
- OS/2 paths (`C:\path`) are translated to Unix paths by replacing backslashes. OS/2 error codes (u32) are returned in RAX.
- Ensure this document is updated to reflect any changes in the workflow and maintain consistency.

### While Planning, Refactoring & Doing Code Review

- When a feature requirement is unclear or ambiguous, seek clarification on definition and scope rather than guessing.

### While Coding

- **Always** try to write test sample OS/2 applications (in `samples/`) to verify assumptions, evaluations.

### After Every Change

1. **Always** update all relevant documents (`README.md`, `doc/*.md` and this file `CLAUDE.md`...)
2. **Always** add essential but missing tests to improve test coverage and ensure code quality
3. **Always** check if there is any missing or incomplete test
4. **Always** run `cargo clippy -- -D warnings` and fix all warnings before committing
4. Remove the finishied tasks from TODOs
5. When a bug is discovered, **always** check for similar issues across the project after applying the fix

### Release Engineering

When creating a new release:

1. Update `CHANGELOG.md` with the new version entry (follow [Keep a Changelog](https://keepachangelog.com/) format)
2. Update `version` in `Cargo.toml` to match the new tag version
3. Commit, push, and create the git tag (e.g. `v1.1.21`)
4. Push the tag (`git push --tags`)
5. Create the GitHub release via `gh release create`

### Code Organization

- Commit by topic — group related files per commit

### Security Rules

- **Always** take care about hypervisor escape prevention
- Memory safety is top priority
